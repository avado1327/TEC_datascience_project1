---
title: "Densidad del aire"
output:
  html_document:
    df_print: paged
---
```{r}
# install.packages("data.table")
## install.packages("ggplot2")
# library(data.table)
# library(ggplot2)
library(hms) # Para tratar con tiempos en formato hora:minuto:segundos
library(dplyr) # Para facilitar la manipulación y operaciones con dataframes
library(ggpubr)
library(plotly)
library(outliers)
library(car)
library(mclust)
library(moments)
library(fitdistrplus) 
```

En este cuaderno de R se busca implementar una estimación de la densidad del aire con su incertidumbre en el laboratorio de balanzas del DMQ del LCM. Para esto se cuentan con datos de cuatro años de las condiciones ambientales del laboratorio.

Deberá implementar la estimación de la densidad del aire por medio de la fórmula CIPM-2007 que se encuentra en el artículo *Revised formula for the density of moist air (CIPM-2007)* de A Picard, R S Davis, M Gläser y K Fujii del $2008$, este se puede visitar en https://www.nist.gov/system/files/documents/calibrations/CIPM-2007.pdf.

Para utilizar una constante de los gases ideales más actualizada puede tomarla de https://physics.nist.gov/cgi-bin/cuu/Value?r|search_for=R, pues es el valor más actualizado de acuerdo a las definiciones nuevas de las magnitudes.

Inicialmente, deberá realizar un análisis exploratorio de las magnitudes de interés: temperatura ambiental, presión atmosférica y humedad relativa.

- Realice una discriminación de datos diurnos (8 am a 4 pm) y nocturnos para que el estudio sea más completo.
- Descarte los datos atípicos.
- Analice tendencias, si las hubiera, en las magnitudes de influencia.

Seguidamente, deberá elaborar las funciones que permiten realizar los cálculos de los diversos parámetros que se utilizan en la fórmula del CIPM-2007, para lo cual deberá ser cuidadoso con las unidades, puesto que algunos utilizan la presión en Pa y otros en hPa, así como la temperatura en grados Celsius o en Kelvin.

Para agregar una celda de código nueva utilice ```Ctrl+Alt+I``` y automáticamente se genera el _chunk_ para escribir código R.

<span style="color:blue">**Utilice los espacios entre _chunks_ para comentar los análisis realizados, explicar las funciones utilizadas y explicar las observaciones encontradas**</span>

# Carga de datos

Una forma sencilla de cargar los datos es por medio de un ```for``` que itere sobre listas de directorios o archivos. Pero puede investigar formas más ágiles de hacer este procedimiento (```do.call``` y ```lapply```). La cantidad de datos ronda casi el millón de datos, por lo que se espera que una vez cargados la visualización sea un tanto lenta.

```{r}
filedir="Datos/CondicionesAmbientalesBalanzas" # directorio de datos
años=list.dirs(path = filedir, full.names = TRUE, recursive = TRUE)[2:5] # lista de directorios
filedir="Datos/CondicionesAmbientalesBalanzas" # directorio de datos
años=list.dirs(path = filedir, full.names = TRUE, recursive = TRUE)[2:5] # lista de directorios

for (año in 1:4) {
  filesAnuales=list.files(path = años[año], full.names = TRUE, recursive = TRUE)
    for (file in 1:length(filesAnuales)){
      if (año==1 & file==1){
        df = read.csv(file = filesAnuales[1], header=FALSE, sep="",dec=".", fill = TRUE, fileEncoding="latin1") 
      }
      else{
        df_i = read.csv(file = filesAnuales[file], header=FALSE, sep="",dec=".", fill = TRUE, fileEncoding="latin1") 
        df <- rbind(df,df_i) 
      }
    }
}
colnames(df) <- c("Fecha","Hora","Temperatura","°C","Humedad","%","Presión","hPa","Densidad","g/cm3","x","y","z")


df$Hora <- as_hms(df$Hora)
df = mutate(df, Horario = if_else(Hora > as_hms("7:59:59") & Hora < as_hms("16:00:00"), "Diurno", "Nocturno"))
```

# Análisis exploratorio de las magnitudes de interés

Aplique las técnicas más convenientes según su criterio de analista a los datos de las magnitudes de interés.

```{r}
# Utilice las herramientas que hasta el momento ha aprendido en el curso para analizar la temperatura ambiental, la presión atmosférica y la humedad relativa.

```

Primero se separan los datos diurnos y nocturnos.

```{r}
df_diurno <- subset(df, Horario == "Diurno")
df_nocturno <- subset(df, Horario == "Nocturno")
```

Para el análisis de temperatura. Primero se realiza el gráfico de cajas.

```{r}

{boxplot(df_diurno$Temperatura, col= "light gray", horizontal=TRUE, axes=FALSE, xlab="Temperatura ambiente para horario diurno")
  axis(1)}

{boxplot(df_nocturno$Temperatura, col= "light gray", horizontal=TRUE, axes=FALSE, xlab="Temperatura ambiente para horario nocturno")
  axis(1)}
```

En ambos gráficos se observan 4 o 5 datos alejados del límite inferior del boxplot y algunos por encima del límite superior. Se trata de datos atípicos, por lo tanto se eliminan.

```{r}
#Para horario diurno
quartiles_td <- quantile(df_diurno$Temperatura, probs=c(.25, .75), na.rm = FALSE)
IQR_td <- IQR(df_diurno$Temperatura)

Lower <- quartiles_td[1] - 1.5*IQR_td
Upper <- quartiles_td[2] + 1.5*IQR_td

tempDOut <- subset(df_diurno$Temperatura, df_diurno$Temperatura > Lower & df_diurno$Temperatura < Upper)
boxplot(tempDOut, horizontal = T, xlab = "Temperatura en horario diurno")

#Para horario nocturno
quartiles_tn <- quantile(df_nocturno$Temperatura, probs=c(.25, .75), na.rm = FALSE)
IQR_tn <- IQR(df_nocturno$Temperatura)

Lower <- quartiles_tn[1] - 1.5*IQR_tn
Upper <- quartiles_tn[2] + 1.5*IQR_tn

tempNOut <- subset(df_nocturno$Temperatura, df_nocturno$Temperatura > Lower & df_nocturno$Temperatura < Upper)
boxplot(tempNOut, horizontal = T, xlab = "Temperatura en horario nocturno")

```
Se presentan el resumen, medidas de ubicación y medida de dispersión de los datos.

```{r}
#Diurno
print("Diurno:")
summary(tempDOut)
var(tempDOut) #Varianza
sd(tempDOut) #Desviación estándar
error_estandar <- sd(tempDOut)/sqrt(length((tempDOut)))
error_estandar #Calcula el error estándar
cv <- sd(tempDOut)/mean(tempDOut) *100
cv #Coeficiente de variación.
skewness(tempDOut) #Asimetría
kurtosis(tempDOut) #kurtosis

#Nocturno

print("Nocturno:")
summary(tempNOut)
var(tempNOut) #Varianza
sd(tempNOut) #Desviación estándar
error_estandar <- sd(tempNOut)/sqrt(length((tempNOut)))
error_estandar #Calcula el error estándar
cv <- sd(tempNOut)/mean(tempNOut) *100
cv #Coeficiente de variación.
skewness(tempNOut) #Asimetría
kurtosis(tempNOut) #kurtosis


```
Se observa que en ambos, la mediana es menos a la media, lo que indica que los datos tienen asimetría hacia la derecha. Ambos conjuntos de datos son muy similares en sus mediciones de ubicación.

```{r}
par(mfrow = c(1, 2))
qqPlot(tempDOut, ylab="Temperatura en horario diurno")
qqPlot(tempNOut, ylab="Temperatura en horario nocturno")
```
Por lo tanto, se descarta la normalidad de los datos para ambas distribuciones.

Se grafica el histograma para ver la distribución de los datos.

```{r}
ref_value <- mean(df$Temperatura)
sigma <- sd(df$Temperatura)
inf <- ref_value - 2*sigma
sup <- ref_value + 2*sigma

hist(tempDOut, 
     col="light gray", 
     main="",
     prob=T,
     density=20, 
     xlab="Distribución temperatura en horario diurno", 
     breaks = 100, 
     xlim = c(16.5, 22.5))
lines(density(tempDOut, bw=0.05), col="blue", lwd=2)
points(ref_value,0, pch=19, col="blue", cex = 2)
segments(inf,0, ref_value,0, col = "blue",lwd=4)
segments(ref_value,0, sup,0, col = "blue",lwd=4)

hist(tempNOut, 
     col="light gray", 
     main="",
     prob=T,
     density=20, 
     xlab="Distribución temperatura en horario nocturno", 
     breaks = 100, 
     xlim = c(16.5, 22.5))
lines(density(tempNOut, bw=0.05), col="blue", lwd=2)
points(ref_value,0, pch=19, col="blue", cex = 2)
segments(inf,0, ref_value,0, col = "blue",lwd=4)
segments(ref_value,0, sup,0, col = "blue",lwd=4)
```
De los histogramas se concluyen dos cosas. La primera es que el horario no influye mucho en la distribución, ya que gráficamente esta se ve muy similar para ambos horarios. La segunda es que parece que sigue una distribución bimodal. A continuación se va a estudiar las mediciones de dispersión para un modelo mixto.

```{r}
library(mixtools)
#Diurno
print("Diurno")
set.seed(2) 
tempDOut_bi <- normalmixEM(tempDOut, k = 2)
summary(tempDOut_bi)
#Nocturno
print("Nocturno")
tempNOut_bi <- normalmixEM(tempDOut, k = 2)
summary(tempNOut_bi)
```

Los estadísticos muestran que no hay diferencia amplia entre los horarios diurno y nocturno, por lo que se puede analizar un solo conjunto de datos.

Al estudiar el modelo mixto, con 2 modas, se tiene que una se ubica cerca de 17.81 °C con desviación estándar de 0.34 °C, mientras que la segunda media está cerca de 19.58 °C con desviación estándar de 0.82 °C proximadamente. Esta función no dice si ambas poblaciones se distribuyen normalmente. Gráficamente se concluye que no, porque la cola derecha tiene mucha frecuencia.

Ahora se grafica la ubicación de la dispersión para ambos grupos.

```{r}
#Solo se analiza un horario, ya que se determinó que el horario no afecta la distribución de los datos
mu.1<- tempDOut_bi$mu[[1]]
mu.2<- tempDOut_bi$mu[[2]]

sigma.1<- tempDOut_bi$sigma[[1]]
sigma.2<- tempDOut_bi$sigma[[2]]

IC.inf.1 <- mu.1+sigma.1
IC.sup.1 <- mu.1-sigma.1
IC.inf.2 <- mu.2+sigma.2
IC.sup.2 <- mu.2-sigma.2

hist(tempDOut, prob=T,density=20, ylab = "Densidad", xlab = "Temperatura ambiente (°C)", main = "")
lines(density(tempDOut), col="blue", lwd=2)
points(mu.1,0, pch=19, col="blue", cex = 2)
points(mu.2,0, pch=19, col="blue", cex = 2)
segments(IC.inf.1,0, mu.1,0, col = "blue",lwd=4)
segments(mu.1,0, IC.sup.1,0, col = "blue",lwd=4)
segments(IC.inf.2,0, mu.2,0, col = "blue",lwd=4)
segments(mu.2,0, IC.sup.2,0, col = "blue",lwd=4)

```

Para estudiar si ambas modas se distribuyen de forma normal. No se puede utilizar el test de shapiro, ya que tiene más de 5000 datos. Tampoco el de Alderson- Darling, este porque no carga. Se prueba gráficamente con la función mclustBIC.

```{r}
library(mclust)
yBIC = mclustBIC(tempDOut, modelNames="V")
yModel = mclustModel(tempDOut, yBIC)
yModel$parameters$mean
yModel$parameters$variance$sigmasq
plot(yModel$z[,1],yModel$z[,2], ylab = "Moda 1", xlab = "Moda 2")
```

Se observa que no se ajusta a un modelo normal. De igual manera con el horario nocturno

```{r}
yBIC = mclustBIC(tempNOut, modelNames="V")
yModel = mclustModel(tempNOut, yBIC)
yModel$parameters$mean
yModel$parameters$variance$sigmasq
plot(yModel$z[,1],yModel$z[,2], ylab = "Moda 1", xlab = "Moda 2")
```

Se concluye que la distribución de la temperatura no depende del nocturno o diurno. La temperatura tiene una tendencia de mayor concentración en 17.81 °C o 19.58 °C, se tiene que analizar las condiciones del laboratorio para determinar mejor cómo separar mejor los datos para mejorar el análisis.

Ahora para la presión.
La presión está reportada en kPa o hPa. Se necesita trabajar en la misma unidad, por lo que se hace la conversión.

```{r}
df_diurno$Presión <- ifelse(df_diurno$hPa == "kPa", df_diurno$Presión * 10, df_diurno$Presión)
df_nocturno$Presión <- ifelse(df_nocturno$hPa == "kPa", df_nocturno$Presión * 10, df_nocturno$Presión)
```
Se hace el gráfico de cajas

```{r}
{boxplot(df_diurno$Presión, col= "light gray", horizontal=TRUE, axes=FALSE, xlab="Presión para horario diurno")
  axis(1)}

{boxplot(df_nocturno$Presión, col= "light gray", horizontal=TRUE, axes=FALSE, xlab="Presión para horario nocturno")
  axis(1)}
```
Los datos atípicos no permiten ver el rango del boxplot. Se eliminan los datos atípicos.

```{r}
#Para horario diurno
quartiles_pd <- quantile(df_diurno$Presión, probs=c(.25, .75), na.rm = FALSE)
IQR_pd <- IQR(df_diurno$Presión)

Lower <- quartiles_pd[1] - 1.5*IQR_pd
Upper <- quartiles_pd[2] + 1.5*IQR_pd

presDOut <- subset(df_diurno$Presión, df_diurno$Presión > Lower & df_diurno$Presión < Upper)
boxplot(presDOut, horizontal = T, xlab="Presión para horario diurno (hPa)")

#Para horario nocturno
quartiles_pn <- quantile(df_nocturno$Presión, probs=c(.25, .75), na.rm = FALSE)
IQR_pn <- IQR(df_nocturno$Presión)

Lower <- quartiles_pn[1] - 1.5*IQR_pn
Upper <- quartiles_pn[2] + 1.5*IQR_pn

presNOut <- subset(df_nocturno$Presión, df_nocturno$Presión > Lower & df_nocturno$Presión < Upper)
boxplot(presNOut, horizontal = T, xlab="Presión para horario nocturno (hPa)")
```

Buscamos el resumen para ambos datos

```{r}
print("Presión diurno:")
summary(presDOut)
print("Presión nocturno:")
summary(presNOut)
```

Hacemos el histograma para ver la dispersión de los datos.

```{r}
#Diurno
mediaPD <- mean(df_diurno$Presión)
sigmaPD <- sd(df_diurno$Presión)
inf <- mediaPD - 2*sigmaPD
sup <- mediaPD + 2*sigmaPD

hist(presDOut, 
     col="light gray", 
     main="",
     prob=T,
     density=20, 
     ylab="Densidad",
     xlab="Distribución presión en horario diurno", 
     breaks = 50)
lines(density(presDOut, bw=0.2), col="blue", lwd=2)
points(mediaPD,0, pch=19, col="blue", cex = 2)
segments(inf,0, mediaPD,0, col = "blue",lwd=4)
segments(mediaPD,0, sup,0, col = "blue",lwd=4)

#Nocturno

mediaPN <- mean(df_nocturno$Presión)
sigmaPN <- sd(df_nocturno$Presión)
inf <- mediaPN - 2*sigmaPN
sup <- mediaPN + 2*sigmaPN

hist(presNOut, 
     col="light gray", 
     main="",
     prob=T,
     density=20, 
     ylab="Densidad",
     xlab="Distribución presión en horario nocturno", 
     breaks = 50)
lines(density(presNOut, bw=0.2), col="blue", lwd=2)
points(mediaPN,0, pch=19, col="blue", cex = 2)
segments(inf,0, mediaPN,0, col = "blue",lwd=4)
segments(mediaPN,0, sup,0, col = "blue",lwd=4)

```

Del histograma, la presión para ambos grupos parece que se distribuye normalmente.

Algunas medidas de dispersión: varianza, desviación estándar, error estándar, coeficiente de variación, asimetría, kurtosis; respectivamente.

```{r}
print("Diurno:")
var(presDOut) #Varianza
sd(presDOut) #Desviación estándar
error_estandar <- sd(presDOut)/sqrt(length((presDOut)))
error_estandar #Calcula el error estándar
cv <- sd(presDOut)/mean(presDOut) *100
cv #Coeficiente de variación.
skewness(presDOut) #Asimetría
kurtosis(presDOut) #kurtosis



print("Nocturno:")
var(presNOut) #Varianza
sd(presNOut) #Desviación estándar
error_estandar <- sd(presNOut)/sqrt(length((presNOut)))
error_estandar #Calcula el error estándar
cv <- sd(presNOut)/mean(presNOut) *100
cv #Coeficiente de variación.
skewness(presNOut) #Asimetría
kurtosis(presNOut) #kurtosis

```


Se hace una prueba de normalidad

```{r}
qqPlot(presDOut, ylab = "Diurno. Presión (hPa)")
qqPlot(presNOut, ylab = "Nocturno. Presión (hPa)")
```
Ahora se ajusta la presión a una distribución normal.
```{r}
fit.norm.presD <- fitdist(presDOut, distr = "norm")
plot(fit.norm.presD)
summary(fit.norm.presD)

fit.norm.presN <- fitdist(presNOut, distr = "norm")
plot(fit.norm.presN)
summary(fit.norm.presD)
```

Se evidencia que no sigue una distribución normal. Sin embargo, el modelo ajustado sí lo sigue.

Para el análisis de la humedad relativa. Primero se hace el gráfico de cajas.

```{r}
{boxplot(df_diurno$Humedad, col= "light gray", horizontal=TRUE, axes=FALSE, xlab="Humedad relativa para horario diurno (%)")
  axis(1)}

{boxplot(df_nocturno$Humedad, col= "light gray", horizontal=TRUE, axes=FALSE, xlab="Humedad relativa para horario nocturno (%)")
  axis(1)}
```

Se descartan los valores atípicos.

```{r}
#Para horario diurno
quartiles_hd <- quantile(df_diurno$Humedad, probs=c(.25, .75), na.rm = FALSE)
IQR_hd <- IQR(df_diurno$Humedad)

Lower <- quartiles_hd[1] - 1.5*IQR_hd
Upper <- quartiles_hd[2] + 1.5*IQR_hd

humDOut <- subset(df_diurno$Humedad, df_diurno$Humedad > Lower & df_diurno$Humedad < Upper)
boxplot(humDOut, horizontal = T, xlab="Humedad relativa para horario diurno (%)")

#Para horario nocturno
quartiles_hn <- quantile(df_nocturno$Humedad, probs=c(.25, .75), na.rm = FALSE)
IQR_hn <- IQR(df_nocturno$Humedad)

Lower <- quartiles_hn[1] - 1.5*IQR_hn
Upper <- quartiles_hn[2] + 1.5*IQR_hn

humNOut <- subset(df_nocturno$Humedad, df_nocturno$Humedad > Lower & df_nocturno$Humedad < Upper)
boxplot(humNOut, horizontal = T, xlab="Humedad relativa para horario nocturno (%)")
```

Parece que los datos están centrados. Viendo algunas medidas de ubicación:

```{r}
#Diurno
print("Resumen diurno")
summary(humDOut)

#NOcturno
print("Resumen nocturno")
summary(humNOut)
```


Se hace un histograma para ver la dispersión de la humedad relativa

```{r}
#Diurno
mediaHD <- mean(df_diurno$Humedad)
sigmaHD <- sd(df_diurno$Humedad)
inf <- mediaHD - 2*sigmaHD
sup <- mediaHD + 2*sigmaHD

hist(humDOut, 
     col="light gray", 
     main="",
     prob=T,
     density=20, 
     ylab="Densidad",
     xlab="Distribución de la humedad relativa en horario diurno", 
     breaks = 50)
lines(density(humDOut, bw=0.2), col="blue", lwd=2)
points(mediaHD,0, pch=19, col="blue", cex = 2)
segments(inf,0, mediaHD,0, col = "blue",lwd=4)
segments(mediaHD,0, sup,0, col = "blue",lwd=4)

#Nocturno

mediaHN <- mean(df_nocturno$Humedad)
sigmaHN <- sd(df_nocturno$Humedad)
inf <- mediaHN - 2*sigmaHN
sup <- mediaHN + 2*sigmaHN

hist(humNOut, 
     col="light gray", 
     main="",
     prob=T,
     density=20, 
     ylab="Densidad",
     xlab="Distribución de la humedad relativa en horario nocturno", 
     breaks = 50)
lines(density(humNOut, bw=0.2), col="blue", lwd=2)
points(mediaHN,0, pch=19, col="blue", cex = 2)
segments(inf,0, mediaHN,0, col = "blue",lwd=4)
segments(mediaHN,0, sup,0, col = "blue",lwd=4)

```

Por encima parece que sigue la distribución de una campana.Visualmente no se evidencia asimetría.

Algunas medidas de dispersión: varianza, desviación estándar, error estándar, coeficiente de variación, asimetría, kurtosis; respectivamente.

```{r}
print("Diurno:")
var(humDOut) #Varianza
sd(humDOut) #Desviación estándar
error_estandar <- sd(humDOut)/sqrt(length((humDOut)))
error_estandar #Calcula el error estándar
cv <- sd(humDOut)/mean(humDOut) *100
cv #Coeficiente de variación.
skewness(humDOut) #Asimetría
kurtosis(humDOut) #kurtosis



print("Nocturno:")
var(humNOut) #Varianza
sd(humNOut) #Desviación estándar
error_estandar <- sd(humNOut)/sqrt(length((humNOut)))
error_estandar #Calcula el error estándar
cv <- sd(humNOut)/mean(humNOut) *100
cv #Coeficiente de variación.
skewness(humNOut) #Asimetría
kurtosis(humNOut) #kurtosis
```

Ahora se hace una prueba de normalidad

```{r}
qqPlot(humDOut, ylab = "Diurno. Humedad (%)")
qqPlot(humNOut, ylab = "Nocturno. Humedad (%)")
```

Se evidencia que la humedad no sigue una distribución normal. Ahora se utiliza el modelo de ajuste lognormal.


```{r}
fit.lnorm <- fitdist(humDOut, distr = "lnorm", method = "mle")
plot(fit.lnorm)

fit.lnorm <- fitdist(humNOut, distr = "lnorm", method = "mle")
plot(fit.lnorm)
```

Vemos que solo el Q-Q pLot no coincide con el teórico.


# Analisis de Magnitudes por Año
Se realizara el analisis de las magnitudes de interes por en cada año correspondiente:
```{r}
#Creamos las variables de los datos anuales 

df_2019<- df %>% filter(grepl("9$", Fecha))
df_2020<- df %>% filter(grepl("0$", Fecha))
df_2021<- df %>% filter(grepl("1$", Fecha))
df_2022<- df %>% filter(grepl("2$", Fecha))
#> summary(subset(df_2019, select = c("Temperatura","Humedad","Presión","Densidad")))
```

##Año 2019

```{r}

main_2019=summary(subset(df_2019, select = c("Temperatura","Humedad","Presión","Densidad")))
tabla_2019=as.matrix(main_2019)
print(tabla_2019)
```
En esta tabla podemos ver los valores estadisticos mas importantes para las variables mostradas durante el año 2019, los cuales podemos ver mas adelante mediante un boxplot para cada una de estas magnitudes.

```{r}

par(mfrow = c(2, 2))
boxplot(df_2019$Densidad, main = "Valores Densidad 2019")
boxplot(df_2019$Presión, main = "Valores Presion 2019")
boxplot(df_2019$Humedad, main = "Valores Humedad 2019")
boxplot(df_2019$Temperatura, main = "Valores Temperatura 2019")


```

Ahora analizaremos el mismo comportamiento pero utilizando un histograma y ver la distribucion que estos datos toman, como estos datos nos interesa analizar mas a detalle primero se realizara un hitograma para cada magnitud. y posterior una comparativa de todos.

```{r}


fig=plot_ly(df_2019,x = df_2019$Densidad, type = "histogram", name = "Histogram") %>% 
layout(title = "Histograma Valores Densidad 2019", xaxis = list(title = "Valores Densidad [g/cm³]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2019,x = df_2019$Presión, type = "histogram", name = "Histogram") %>% 
layout(title = "Histograma Valores Presion 2019", xaxis = list(title = "Valores Presion [hPa]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2019,x = df_2019$Humedad, type = "histogram", name = "Histogram") %>% 
layout(title = "Histograma Valores Humedad 2019", xaxis = list(title = "Valores Humedad [%]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2019,x = df_2019$Temperatura, type = "histogram", name = "Histogram") %>% 
layout(title = "Histograma Valores Temperatura 2019", xaxis = list(title = "Valores Temperatura [°C]"),yaxis = list(title = "Frecuencia"))
fig

```


```{r}
fig= subplot(
  plot_ly(df_2019, y = ~Densidad, type = "histogram",name="Valores Densidad 2019", orientation = "H"),
  plot_ly(df_2019, y = ~Presión , type = "histogram",name="Valores Presion 2019", orientation = "H"),
  plot_ly(df_2019, y = ~Humedad, type = "histogram",name="Valores Humedad 2019", orientation = "H"), 
  plot_ly(df_2019, y = ~Temperatura, type = "histogram",name="Valores Temperatura 2019", orientation = "H"),
  nrows = 2,
  margin = 0.05
) %>%
  layout(title = "Comparativa Magnitudes 2019")
fig
```




```{r}

ggqqplot(df_2019$Densidad, title = 'Q-Q Plot Densidad en 2019')

ggqqplot(df_2019$Temperatura, title = 'Q-Q Plot Temperatura en 2019')

ggqqplot(df_2019$Presión, title = 'Q-Q Plot Presion en 2019'
         )
ggqqplot(df_2019$Humedad, title = 'Q-Q Plot Humedad en 2019')

```

## Año 2020

```{r}
main_2020=summary(subset(df_2020, select = c("Temperatura","Humedad","Presión","Densidad")))
tabla_2020=as.matrix(main_2020)
print(tabla_2020)
```


```{r}


par(mfrow = c(2, 2))
boxplot(df_2020$Densidad, main = "Valores Densidad 2020")
boxplot(df_2020$Presión, main = "Valores Presion 2020")
boxplot(df_2020$Humedad, main = "Valores Humedad 2020")
boxplot(df_2020$Temperatura, main = "Valores Temperatura 2020")
```



```{r}


fig=plot_ly(df_2020,x = df_2020$Densidad, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Densidad 2020", xaxis = list(title = "Valores Densidad [g/cm³]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2020,x = df_2020$Presión, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Presion 2020", xaxis = list(title = "Valores Presion [hPa]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2020,x = df_2020$Humedad, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Humedad 2020", xaxis = list(title = "Valores Humedad [%]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2020,x = df_2020$Temperatura, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Temperatura 2020", xaxis = list(title = "Valores Temperatura [°C]"),yaxis = list(title = "Frecuencia"))
fig

```


```{r}
fig= subplot(
  plot_ly(df_2020, y = ~Densidad, type = "histogram",name="Valores Densidad 2020", orientation = "H"),
  plot_ly(df_2020, y = ~Presión , type = "histogram",name="Valores Presion 2020", orientation = "H"),
  plot_ly(df_2020, y = ~Humedad, type = "histogram",name="Valores Humedad 2020", orientation = "H"), 
  plot_ly(df_2020, y = ~Temperatura, type = "histogram",name="Valores Temperatura 2020", orientation = "H"),
  nrows = 2,
  margin = 0.05
) %>%
  layout(title = "Comparativa Magnitudes 2020")
fig
```
Como se pudever en los histogramas presentados anteriormente, se puede observar que el comportamiento de los datos, exceptuando aquellos de Presion, no poseen un comportamiento que se asemeje a una distribucion normal, esto nos permite inferir que al menos de momento, se puede presentar una distribucion bimodal, la cual es la responsable del comportamiento observado en los datos tomados durante el ano.

Podemos observar esto realizando Q-Q Plots para verificar esto.

```{r}



ggqqplot(df_2020$Densidad, title = 'Q-Q Plot Densidad en 2020')

ggqqplot(df_2020$Temperatura, title = 'Q-Q Plot Temperatura en 2020')

ggqqplot(df_2020$Presión, title = 'Q-Q Plot Presion en 2020'
)
ggqqplot(df_2020$Humedad, title = 'Q-Q Plot Humedad en 2020')


```
A continuacion se hara el mismo procedimiento con los datos para los demas años

## Año 2021


```{r}
main_2021=summary(subset(df_2021, select = c("Temperatura","Humedad","Presión","Densidad")))
tabla_2021=as.matrix(main_2021)
print(tabla_2021)
```

```{r}

par(mfrow = c(2, 2))
boxplot(df_2021$Densidad, main = "Valores Densidad 2021")
boxplot(df_2021$Presión, main = "Valores Presion 2021")
boxplot(df_2021$Humedad, main = "Valores Humedad 2021")
boxplot(df_2021$Temperatura, main = "Valores Temperatura 2021")

```



```{r}


fig=plot_ly(df_2021,x = df_2021$Densidad, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Densidad 2021", xaxis = list(title = "Valores Densidad [g/cm³]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2021,x = df_2021$Presión, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Presion 2021", xaxis = list(title = "Valores Presion [hPa]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2021,x = df_2021$Humedad, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Humedad 2021", xaxis = list(title = "Valores Humedad [%]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2021,x = df_2021$Temperatura, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Temperatura 2021", xaxis = list(title = "Valores Temperatura [°C]"),yaxis = list(title = "Frecuencia"))
fig

```


```{r}
fig= subplot(
  plot_ly(df_2021, y = ~Densidad, type = "histogram",name="Valores Densidad 2021", orientation = "H"),
  plot_ly(df_2021, y = ~Presión , type = "histogram",name="Valores Presion 2021", orientation = "H"),
  plot_ly(df_2021, y = ~Humedad, type = "histogram",name="Valores Humedad 2021", orientation = "H"), 
  plot_ly(df_2021, y = ~Temperatura, type = "histogram",name="Valores Temperatura 2021", orientation = "H"),
  nrows = 2,
  margin = 0.05
) %>%
  layout(title = "Comparativa Magnitudes 2021")
fig
```


```{r}

ggqqplot(df_2021$Densidad, title = 'Q-Q Plot Densidad en 2021')

ggqqplot(df_2021$Temperatura, title = 'Q-Q Plot Temperatura en 2021')

ggqqplot(df_2021$Presión, title = 'Q-Q Plot Presion en 2021')

ggqqplot(df_2021$Humedad, title = 'Q-Q Plot Humedad en 2021')

```

## Año 2022

```{r}

main_2022=summary(subset(df_2022, select = c("Temperatura","Humedad","Presión","Densidad")))
tabla_2022=as.matrix(main_2022)
print(tabla_2022)

```


```{r}

par(mfrow = c(2, 2))
boxplot(df_2022$Densidad, main = "Valores Densidad 2022")
boxplot(df_2022$Presión, main = "Valores Presion 2022")
boxplot(df_2022$Humedad, main = "Valores Humedad 2022")
boxplot(df_2022$Temperatura, main = "Valores Temperatura 2022")


```



```{r}


fig=plot_ly(df_2022,x = df_2022$Densidad, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Densidad 2022", xaxis = list(title = "Valores Densidad [g/cm³]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2022,x = df_2022$Presión, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Presion 2022", xaxis = list(title = "Valores Presion [hPa]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2022,x = df_2022$Humedad, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Humedad 2022", xaxis = list(title = "Valores Humedad [%]"),yaxis = list(title = "Frecuencia"))
fig

fig=plot_ly(df_2022,x = df_2022$Temperatura, type = "histogram", name = "Histogram") %>% 
  layout(title = "Histograma Valores Temperatura 2022", xaxis = list(title = "Valores Temperatura [°C]"),yaxis = list(title = "Frecuencia"))
fig

```


```{r}
fig= subplot(
  plot_ly(df_2022, y = ~Densidad, type = "histogram",name="Valores Densidad 2022", orientation = "H"),
  plot_ly(df_2022, y = ~Presión , type = "histogram",name="Valores Presion 2022", orientation = "H"),
  plot_ly(df_2022, y = ~Humedad, type = "histogram",name="Valores Humedad 2022", orientation = "H"), 
  plot_ly(df_2022, y = ~Temperatura, type = "histogram",name="Valores Temperatura 2022", orientation = "H"),
  nrows = 2,
  margin = 0.05
) %>%
  layout(title = "Comparativa Magnitudes 2022")
fig
```



```{r}



ggqqplot(df_2022$Densidad, title = 'Q-Q Plot Densidad en 2022')

ggqqplot(df_2022$Temperatura, title = 'Q-Q Plot Temperatura en 2022')

ggqqplot(df_2022$Presión, title = 'Q-Q Plot Presion en 2022'
)
ggqqplot(df_2022$Humedad, title = 'Q-Q Plot Humedad en 2022')

```


# Funciones para el cálculo de la densidad del aire

Para calcular la densidad del aire utilizando la fórmula del CIPM 2007 es necesario tener sumo cuidado con las unidades. Puesto que para calcular algunos parámetros las fórmulas utilizan la temperatura en ºC o en K, la presión atmosférica en hPa o en Pa y la humedad relativa como un valor entre 0 y 1 o como un valor entre 0 y 100.

Puede realizar los cambios que considere pertinentes

```{r}
Psv=function(t){
  # Función que calcula la presión de vapor de saturación siguiendo la fórmula que se presenta en el artículo de referencia.
  #Input:
  #-------------
  #t: temperatura en °C.
    
  #Output:
  #-------------
  #Psv: valor de la presión de vapor de saturación en hPa.
  
  A = 1.2378847 * 10^(-5) #K^-2
  B = -1.9121316 * 10^(-2) #K^-1
  C = 33.93711047
  D = -6.3431645 * 10^(3) #K
  Tk = 273.15 + t #K
  Psv = 1 * exp(A*(Tk)^2 + B*Tk + C + D/Tk) 
  return(Psv/100)
}
```
```{r}
f=function(t,P){
  # Función que calcula el factor de fugasidad siguiendo la ecuación de la referencia.
    
  # Input:
  # --------------
  # P: presión en Pa.
  # t: temperatura en °C.
  
  # Output:
  # --------------
  # f: Valor del coeficiente de fugacidad.

  alpha = 1.00062
  beta = 3.14 * 10^(-8) #Pa^(-1)
  gamma = 5.6 * 10^(-7) #K^(-2)
  
  f = alpha + beta*P + gamma*(t^2)

  return(f)
}
```
```{r}
xv=function(t,P,h){
  # Función que calcula la fracción molar de vapor de agua siguiendo el artículo de referencia.
    
  # Input:
  # ---------------
  # h: humedad relativa como un valor entre 0 y 1
  # P: presión atmosférica en Pa.
  # t: Temperatura en °C.
  
  # Output:
  # ---------------
  # xv: Valor de la fracción molar de vapor de agua.
  f = f(t,P)
  Psv = Psv(t)
  xv = (h*f*Psv)/(P)
  return(xv)
}
```
```{r}
Z=function(t,P,h){
  # Función que calcula el factor de compresibilidad con la ecuación del artículo de referencia.
    
  # Input:
  # --------------
  # P: Presión atmosférica en Pa.
  # h: Humedad relativa como un valor entre 0 y 1.
  # t: Temperatura en °C.
  
  # Output:
  # ---------------
  # Z: factor de compresibilidad.

  a0 = 1.58123 * 10^(-6) #KPa^(-1)
  a1 = -2.9331 * 10^(-8) #Pa^(-1)
  a2 = 1.1043 * 10^(-10) #K^(-1)Pa^(-1) 
  b0 = 5.707 * 10^(-6) #KPa^(-1)
  b1 = -2.051 * 10^(-8) #Pa^(-1)
  c0 = 1.9898 * 10^(-4) #KPa^(-1)
  c1 = -2.376 * 10^(-6) #Pa^(-1)
  d = 1.83 * 10^(-11) #K^(2)Pa^(-2)
  e = -0.765 * 10^(-8) #K^(2)Pa^(-2)
  
  xv = xv(t,P,h)
  Tk = 273.15 + t
  Z = 1 - P*(a0 + a1*t + a2*(t^2) + (b0 + b1*t)*xv + (c0 + c1*t)*(xv^2))/(Tk) + P^2*(d + e*(xv^2))/(Tk^2)

  return(Z)
}
```
```{r}
DensidadCIPM=function(t,P,h){
  # Función que calcula la densidad del aire a partir de la ecuación del artículo de referencia.
    
  # Input:
  # --------------
  # P: Presión atmosférica en Pa.
  # h: Humedad relativa como un valor entre 0 y 1.
  # t: Temperatura en °C.
  
  # Output:
  # --------------
  # rho: densidad del aire en kg/m^3.

  Ma = 28.96546 * 10^(-3) #kg mol^(-1)
  Mv = 18.01528 * 10^(-3) #kg mol^(-1)
  R = 8.314462618 #J mol^(-1) K^(-1)
  Tk = 273.15 + t #K
  Z = Z(t,P,h)
  xv = xv(t,P,h)
  
  rho = (P*Ma)/(Z*R*Tk) * (1 - xv*(1 - (Mv/Ma)))
  return(rho)
}
```
```{r}
# Calcule las densidades
DensidadDf=function(t,P_hpa,h_100){
  # Función que calcula la densidad del aire a partir de la ecuación del artículo de referencia, considerando unidades usadas en el dataframe.
    
  # Input:
  # --------------
  # P_hpa: Presión atmosférica en hPa.
  # h_100: Humedad relativa como un valor entre 0 y 100.
  # t: Temperatura en °C.
  
  # Output:
  # --------------
  # rho: densidad del aire en g/cm^3.
  
  P = P_hpa * 100 #Presión en Pa
  h = h_100 / 100 #Humedad relativa como valor entre 0 y 1
  rho = DensidadCIPM(t,P,h) / 1000 #Densidad del aire en g/cm^3
  return(rho)
}
Dens_calc=DensidadDf(df$Temperatura,df$Presión,df$Humedad)
df <- as.data.frame(cbind(df,Dens_calc))

```
# Análisis exploratorio de las densidades calculadas

Análisis por horario (diurno/nocturno): 

```{r}

df_noc <- na.omit(filter(df,Horario == 'Nocturno'))
df_dia <- na.omit(filter(df,Horario == 'Diurno'))

ggqqplot(df_noc$Dens_calc, title = 'Q-Q Plot densidad en horario nocturno')
ggqqplot(df_dia$Dens_calc, title = 'Q-Q Plot densidad en horario diurno')
```

Los gráficos anteriores muestran como en ambos casos, existen valores que se alejan en gran medida de la muestra general. Valores menores a 0,001025 g/cm^3 que no tienen sentido en el contexto actual ya que corresponden a valores que por lo general no serían comunes de medir en el espacio experimental donde se determinaron las condiciones ambientales. Según la linea de normalidad, estos datos son los que menos se ajustan también a la normalidad de la muestra. Por tanto, los mismos pueden considerarse datos atípicos y deben ser eliminados. 
```{r}
df_noc1 <- filter(df_noc,Densidad>0.001024)
df_dia1 <- filter(df_dia,Densidad>0.001024)
ggqqplot(df_noc1$Densidad, title = 'Q-Q Plot densidad en horario nocturno (1er filtro)')
ggqqplot(df_dia1$Densidad, title = 'Q-Q Plot densidad en horario diurno (1er filtro)')
```
Se utilizará el test de Grubbs para hacer un análisis final de los valores de los extremos.
```{r}
grubbs.test(df_noc1$Densidad, type = 11)
grubbs.test(df_dia1$Densidad, type = 11)
```

Aun se obtiene que los valores de los extremos son Outliers; sin embargo, ya tienen una tendencia cercada a la de la población y son valores posibles para la densidad del aire, por tanto se decide no eliminarlos. Puede notarse también que, a pesar de la eliminación de los valores anómalos, los valores de densidad no siguen una distribución normal. Para conocer como se distribuyen estos datos, se elaborará un histograma.
```{r}
hist(df_noc1$Densidad,breaks = 40, xlab = 'Densidad calculada (horario nocturno) [g/cm^3]')
hist(df_dia1$Densidad, breaks = 40, xlab = 'Densidad calculada (horario diurno) [g/cm^3]')
```

```{r}
summary(df_noc1$Densidad)
summary(df_dia1$Densidad)
```
De lo anterior se puede observar que: (1) La forma de los histogramas claramente no siguen la forma de la distribución normal, tal y como se esperaba por los gráficos Q-Q, (2) El perfil que siguen las columnas de los histogramas presenta, aparentemente, dos máximos locales en aproximadamente 0.001038 g/cm^3 (moda menor) y 0.001047 g/cm^3 (moda mayor) para el caso nocturno y para el caso diurno en 0.001037 g/cm^3 (moda menor, mas sutil que en el caso nocturno) y 0.001047 g/cm^3 (moda mayor), (3) Ambos grupos tienen medias, máximos y mínimos iguales y valores muy similares para sus medianas y cuartiles, (4) Sus distribuciones no son simétricas relativo a sus medias o medianas y tienden a tener una oblicuidad (skewness) negativa, (5) La mayoría de los datos se distribuyen mayormente alrededor de las modas de las distribuciones con una kurtoris positiva.


La similitud de los datos (diurnos y nocturnos) nos lleva a preguntarnos si existen diferencias significativas entre ambos grupos. Según lo que se exploró anteriormente, se podría contestar negativamente respecto a la afirmación sobre la existencia de dichas diferencias significativas, sin embargo, se realizará una prueba estadística para confirmarlo. Como sus distribución, sin lugar a duda, no es normal, se debe realizar una prueba estadística no-paramétrica, en este caso se utilizará el test de Wilcoxon (como es para dos grupos, Test U de Mann-Whitney). Se utilizarán como hipotesis:

$H_{0} =$ Existen diferencias significativas entre los conjuntos de datos
$H_{1} =$ NO existen diferencias significativas entre los conjuntos de datos

```{r}
wilcox.test(df_noc1$Densidad,df_dia1$Densidad)
```
Si se asume un nivel de significancia de $\alpha = 0.05$ entonces se decide rechazar la hipotesis nula y se concluye que no hay diferencias significativas entre la densidad medida en los horarios diurnos y nocturnos. 


Análisis por año

```{r}
df_2019<- df %>% filter(grepl("9$", Fecha))
df_2020<- df %>% filter(grepl("0$", Fecha))
df_2021<- df %>% filter(grepl("1$", Fecha))
df_2022<- df %>% filter(grepl("2$", Fecha))
```


```{r}
df_2019 <- na.omit(df_2019)
df_2020 <- na.omit(df_2020)
df_2021 <- na.omit(df_2021)
df_2022 <- na.omit(df_2022)
```


```{r}
ggqqplot(df_2019$Dens_calc, title = 'Q-Q Plot densidad en 2019')
ggqqplot(df_2020$Dens_calc, title = 'Q-Q Plot densidad en 2020')
ggqqplot(df_2021$Dens_calc, title = 'Q-Q Plot densidad en 2021')
ggqqplot(df_2022$Dens_calc, title = 'Q-Q Plot densidad en 2022')
```
```{r}
df_2020 <- filter(df_2020,Dens_calc>0.001)
ggqqplot(df_2020$Densidad, title = 'Q-Q Plot densidad en 2020 (1er filtro)')
df_2021 <- filter(df_2021,Dens_calc>0.001)
ggqqplot(df_2021$Densidad, title = 'Q-Q Plot densidad en 2021 (1er filtro)')
df_2022 <- filter(df_2022,Dens_calc>0.001)
ggqqplot(df_2022$Densidad, title = 'Q-Q Plot densidad en 2022 (1er filtro)')
```


```{r}
grubbs.test(df_2019$Dens_calc, type = 11)
grubbs.test(df_2020$Dens_calc, type = 11)
grubbs.test(df_2021$Dens_calc, type = 11)
grubbs.test(df_2022$Dens_calc, type = 11)
```

```{r}
hist(df_2019$Dens_calc,breaks = 40, xlab = 'Densidad calculada (2019) [g/cm^3]')
hist(df_2020$Dens_calc,breaks = 40, xlab = 'Densidad calculada (2020) [g/cm^3]')
hist(df_2021$Dens_calc,breaks = 40, xlab = 'Densidad calculada (2021) [g/cm^3]')
hist(df_2022$Dens_calc,breaks = 40, xlab = 'Densidad calculada (2022) [g/cm^3]')
```
```{r}
summary(df_2019$Dens_calc)
summary(df_2020$Dens_calc)
summary(df_2021$Dens_calc)
summary(df_2022$Dens_calc)
```

```{r}

```

